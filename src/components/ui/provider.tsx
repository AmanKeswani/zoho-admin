import React from 'react'

type Props = { children: React.ReactNode }

// Minimal UI provider placeholder for future shadcn/Radix setup
export function UIProvider({ children }: Props) {
  return <>{children}</>
}